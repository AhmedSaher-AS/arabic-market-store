import { StoreLayout } from "@/components/StoreLayout";
import { FavoriteButton } from "@/components/FavoriteButton";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, LoaderCircle, Package, ShoppingBag } from "lucide-react";
import { FormEvent, useEffect, useMemo, useState } from "react";
import { Link, useLocation } from "wouter";

const categories = ["الكل", "كتب", "ملابس", "أجهزة", "متنوعة"] as const;
const paymentMethods = ["فودافون كاش", "فوري", "واتساب", "إنستا باي", "فيزا/ماستركارد", "PayPal"] as const;

export default function LocalProducts() {
  const [location] = useLocation(); const { user, loading } = useAuth(); const { data: products = [], isLoading } = trpc.localProducts.catalog.useQuery(); const purchase = trpc.localProducts.purchase.useMutation();
  const [category, setCategory] = useState<(typeof categories)[number]>("الكل"); const [selectedId, setSelectedId] = useState<number | null>(null); const [customerName, setCustomerName] = useState(""); const [customerPhone, setCustomerPhone] = useState(""); const [shippingAddress, setShippingAddress] = useState(""); const [country, setCountry] = useState("مصر"); const [city, setCity] = useState(""); const [paymentMethod, setPaymentMethod] = useState<(typeof paymentMethods)[number]>("فودافون كاش"); const [orderNumber, setOrderNumber] = useState("");
  const filtered = useMemo(() => category === "الكل" ? products : products.filter(product => product.category === category), [category, products]); const selected = products.find(product => product.id === selectedId);
  useEffect(() => { const value = Number(new URLSearchParams(window.location.search).get("product")); if (value && products.some(product => product.id === value)) setSelectedId(value); }, [location, products]);
  const buy = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!selected || !user) return; const result = await purchase.mutateAsync({ productId: selected.id, customerName, customerPhone, shippingAddress, country, city, paymentMethod }); setOrderNumber(result.orderNumber); };
  return <StoreLayout><section className="border-b border-stone-200 bg-[linear-gradient(135deg,#e3eee8,#fffdf8)]"><div className="container py-12"><p className="text-xs font-extrabold tracking-[0.16em] text-[#2d7a51]">متجر مستقل</p><h1 className="mt-2 text-4xl font-black text-[#173c37]">منتجاتك تُدار من متجرك مباشرة</h1><p className="mt-3 max-w-2xl leading-7 text-stone-600">تصفّح الكتب والملابس والأجهزة التي يضيفها المدير محليًا. تحفظ الأسعار والمخزون من الخادم عند إنشاء الطلب.</p></div></section><div className="container py-10"><div className="mb-7 flex flex-wrap gap-2">{categories.map(item => <Button key={item} type="button" variant={category === item ? "default" : "outline"} onClick={() => setCategory(item)} className={category === item ? "rounded-full bg-[#173c37]" : "rounded-full"}>{item}</Button>)}</div><div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{isLoading ? <div className="col-span-full grid min-h-72 place-items-center"><LoaderCircle className="h-7 w-7 animate-spin text-[#173c37]" /></div> : filtered.length ? filtered.map(product => <article key={product.id} className="overflow-hidden rounded-3xl border border-stone-200 bg-white shadow-[0_12px_30px_rgba(23,60,55,0.06)]">{product.imageUrl ? <img src={product.imageUrl} alt={product.title} className="h-52 w-full object-cover" /> : <div className="grid h-52 place-items-center bg-[#e3eee8] text-[#173c37]"><Package className="h-12 w-12" /></div>}<div className="p-6"><div className="flex items-start justify-between gap-3"><div><span className="text-xs font-bold text-[#2d7a51]">{product.category}</span><h2 className="mt-1 text-xl font-black text-[#173c37]">{product.title}</h2></div><div className="flex items-center"><FavoriteButton compact item={{ itemType: "منتج", itemId: product.id, title: product.title, subtitle: product.description, price: product.price, currencyCode: product.currencyCode, imageUrl: product.imageUrl, targetPath: `/متجر-مستقل?product=${product.id}` }} /><span className="rounded-full bg-[#f7ead6] px-2.5 py-1 text-xs font-black text-[#9a5821]">{Number(product.price).toLocaleString("ar-EG")} {product.currencyCode}</span></div></div><p className="mt-3 min-h-12 text-sm leading-7 text-stone-600">{product.description || "منتج مختار من سوقك العربي."}</p><div className="mt-5 flex items-center justify-between border-t border-stone-100 pt-4"><span className="text-xs text-stone-500">متبقي: {product.inventory}</span><Button onClick={() => { setSelectedId(product.id); setOrderNumber(""); }} className="rounded-xl bg-[#173c37]"><ShoppingBag className="ml-1.5 h-4 w-4" />اطلب الآن</Button></div></div></article>) : <div className="col-span-full rounded-3xl border border-dashed border-stone-300 bg-[#fffdf8] px-6 py-16 text-center"><Package className="mx-auto h-8 w-8 text-[#173c37]" /><h2 className="mt-4 text-xl font-black text-[#173c37]">لا توجد منتجات مطابقة بعد</h2><p className="mt-2 text-sm text-stone-500">سيظهر هنا ما يضيفه مدير المتجر محليًا.</p></div>}</div>{selected && <section className="mt-10 rounded-3xl border border-[#cfe0d9] bg-[#f5faf7] p-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-bold text-[#2d7a51]">طلب محلي مباشر</p><h2 className="mt-1 text-2xl font-black text-[#173c37]">{selected.title}</h2><p className="mt-2 font-extrabold text-[#b76f2c]">{Number(selected.price).toLocaleString("ar-EG")} {selected.currencyCode}</p></div><Button type="button" variant="ghost" onClick={() => { setSelectedId(null); setOrderNumber(""); }} className="text-stone-500">إلغاء</Button></div>{orderNumber ? <div className="mt-6 rounded-2xl bg-white p-5"><CheckCircle2 className="h-7 w-7 text-[#2d7a51]" /><h3 className="mt-3 font-black text-[#173c37]">تم تسجيل طلبك بنجاح</h3><p className="mt-2 text-sm leading-7 text-stone-600">رقم الطلب: <strong>{orderNumber}</strong>. أكمل السداد بالطريقة المختارة ثم ارفع إثبات الدفع من صفحة طلباتك.</p><Button asChild className="mt-5 rounded-xl bg-[#173c37]"><Link href="/طلباتي">متابعة الطلب ورفع الإثبات</Link></Button></div> : loading ? <p className="mt-6 text-sm text-stone-500">جارٍ التحقق من الحساب…</p> : !user ? <div className="mt-6 rounded-2xl bg-white p-5"><p className="text-sm leading-7 text-stone-600">سجّل الدخول لحفظ الطلب وربطه بحسابك ومتابعة الدفع.</p><Button onClick={() => startLogin()} className="mt-4 rounded-xl bg-[#173c37]">تسجيل الدخول</Button></div> : <form onSubmit={buy} className="mt-6 grid gap-4 md:grid-cols-2"><div className="space-y-2"><Label htmlFor="local-product-name">الاسم</Label><Input id="local-product-name" required value={customerName} onChange={event => setCustomerName(event.target.value)} className="h-11 rounded-xl bg-white" /></div><div className="space-y-2"><Label htmlFor="local-product-phone">رقم الهاتف</Label><Input id="local-product-phone" required value={customerPhone} onChange={event => setCustomerPhone(event.target.value)} placeholder="01xxxxxxxxx" className="h-11 rounded-xl bg-white" /></div><div className="space-y-2"><Label htmlFor="local-product-country">الدولة</Label><Input id="local-product-country" required value={country} onChange={event => setCountry(event.target.value)} className="h-11 rounded-xl bg-white" /></div><div className="space-y-2"><Label htmlFor="local-product-city">المدينة</Label><Input id="local-product-city" required value={city} onChange={event => setCity(event.target.value)} className="h-11 rounded-xl bg-white" /></div><div className="space-y-2"><Label htmlFor="local-product-payment">طريقة الدفع</Label><select id="local-product-payment" value={paymentMethod} onChange={event => setPaymentMethod(event.target.value as (typeof paymentMethods)[number])} className="h-11 w-full rounded-xl border border-input bg-white px-3 text-sm">{paymentMethods.map(method => <option key={method}>{method}</option>)}</select></div><div className="space-y-2 md:col-span-2"><Label htmlFor="local-product-address">العنوان بالتفصيل</Label><Textarea id="local-product-address" required value={shippingAddress} onChange={event => setShippingAddress(event.target.value)} className="min-h-20 rounded-xl bg-white" /></div><div className="md:col-span-2"><Button type="submit" disabled={purchase.isPending} className="h-11 rounded-xl bg-[#173c37]">{purchase.isPending && <LoaderCircle className="ml-2 h-4 w-4 animate-spin" />}تسجيل الطلب ومتابعة الدفع</Button>{purchase.error && <p className="mt-3 text-sm text-rose-700">{purchase.error.message}</p>}</div></form>}</section>}</div></StoreLayout>;
}
