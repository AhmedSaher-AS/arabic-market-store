import { DigitalBookUploader } from "@/components/DigitalBookUploader";
import { PaymentOperations } from "@/components/PaymentOperations";
import { StoreContentEditor } from "@/components/StoreContentEditor";
import { StoreLayout } from "@/components/StoreLayout";
import { Button } from "@/components/ui/button";
import { startLogin } from "@/const";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { ExternalLink, LayoutDashboard, LoaderCircle, Package, ShieldCheck, Truck } from "lucide-react";
import { useMemo } from "react";

const statuses = ["معلق", "مؤكد", "مشحون", "مكتمل"] as const;

export default function Admin() {
  const { user, loading } = useAuth();
  const utils = trpc.useUtils();
  const { data: orders = [], isLoading: ordersLoading } = trpc.orders.list.useQuery(undefined, {
    enabled: user?.role === "admin",
  });
  const updateStatus = trpc.orders.updateStatus.useMutation({ onSuccess: () => utils.orders.list.invalidate() });
  const metrics = useMemo(() => ({
    totalOrders: orders.length,
    pendingOrders: orders.filter(order => order.status === "معلق").length,
    fulfilledOrders: orders.filter(order => order.status === "مكتمل").length,
    revenue: orders.reduce((sum, order) => sum + Number(order.total), 0),
  }), [orders]);
  if (loading) return <StoreLayout><div className="container py-24 text-center text-stone-500">جارٍ التحقق من الصلاحيات…</div></StoreLayout>;
  if (!user) return <StoreLayout><div className="container grid min-h-[65vh] place-items-center"><div className="max-w-lg rounded-3xl border border-stone-200 bg-white p-8 text-center"><ShieldCheck className="mx-auto h-8 w-8 text-[#b76f2c]" /><h1 className="mt-4 text-3xl font-black text-[#173c37]">بوابة المدير</h1><p className="mt-3 leading-7 text-stone-600">سجّل الدخول بحساب المالك للوصول إلى أدوات إدارة المتجر.</p><Button onClick={() => startLogin()} className="mt-6 h-12 rounded-xl bg-[#173c37] px-7 font-extrabold">تسجيل الدخول</Button></div></div></StoreLayout>;
  if (user.role !== "admin") return <StoreLayout><div className="container grid min-h-[65vh] place-items-center"><div className="max-w-lg rounded-3xl border border-stone-200 bg-white p-8 text-center"><ShieldCheck className="mx-auto h-8 w-8 text-[#b76f2c]" /><h1 className="mt-4 text-3xl font-black text-[#173c37]">هذه المساحة للمدير فقط</h1><p className="mt-3 leading-7 text-stone-600">استخدم حساب مالك المشروع لإدارة المنتجات والطلبات.</p></div></div></StoreLayout>;
  return <StoreLayout><div className="container py-10"><div className="flex flex-col gap-5 rounded-[32px] bg-[#173c37] p-7 text-white sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm text-[#f5c96a]">لوحة الإدارة</p><h1 className="mt-2 text-3xl font-black">إدارة سوقك العربي</h1><p className="mt-3 text-sm leading-7 text-stone-300">أضف المنتجات، فعّل الدفع والشحن، ثم تابع حالات الطلبات من هنا.</p></div><Button asChild className="rounded-xl bg-[#f5c96a] font-extrabold text-[#173c37] hover:bg-[#edbd53]"><a href="https://admin.shopify.com/" target="_blank" rel="noreferrer">فتح مركز التاجر <ExternalLink className="mr-2 h-4 w-4" /></a></Button></div><section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><div className="rounded-3xl bg-[#e3eee8] p-5"><p className="text-sm font-bold text-stone-600">إجمالي الطلبات</p><strong className="mt-3 block text-3xl font-black text-[#173c37]">{metrics.totalOrders}</strong><p className="mt-2 text-xs text-stone-500">كل الطلبات المسجلة</p></div><div className="rounded-3xl bg-[#f7ead6] p-5"><p className="text-sm font-bold text-stone-600">طلبات معلقة</p><strong className="mt-3 block text-3xl font-black text-[#9a5821]">{metrics.pendingOrders}</strong><p className="mt-2 text-xs text-stone-500">تحتاج مراجعة أو تأكيد دفع</p></div><div className="rounded-3xl bg-[#ebe8f5] p-5"><p className="text-sm font-bold text-stone-600">طلبات مكتملة</p><strong className="mt-3 block text-3xl font-black text-[#50427b]">{metrics.fulfilledOrders}</strong><p className="mt-2 text-xs text-stone-500">اكتملت دورة التنفيذ</p></div><div className="rounded-3xl bg-[#173c37] p-5 text-white"><p className="text-sm font-bold text-stone-300">إجمالي القيمة</p><strong className="mt-3 block text-3xl font-black text-[#f5c96a]">{metrics.revenue.toLocaleString("ar-EG")}</strong><p className="mt-2 text-xs text-stone-300">بالعملة المسجلة في الطلب</p></div></section><div className="mt-7 grid gap-5 md:grid-cols-3"><div className="rounded-3xl border border-stone-200 bg-white p-6"><span className="grid h-11 w-11 place-items-center rounded-xl bg-[#e3eee8] text-[#173c37]"><Package className="h-5 w-5" /></span><h2 className="mt-5 font-black text-[#173c37]">إدارة المنتجات</h2><p className="mt-2 text-sm leading-7 text-stone-500">أضف المنتجات والأسعار والصور والتصنيفات من مركز التاجر، ثم انشرها لتظهر في الكتالوج.</p></div><div className="rounded-3xl border border-stone-200 bg-white p-6"><span className="grid h-11 w-11 place-items-center rounded-xl bg-[#f7ead6] text-[#9a5821]"><Truck className="h-5 w-5" /></span><h2 className="mt-5 font-black text-[#173c37]">إدارة الطلبات</h2><p className="mt-2 text-sm leading-7 text-stone-500">حدّث دورة التنفيذ بالحالات: معلق، مؤكد، مشحون، مكتمل.</p></div><div className="rounded-3xl border border-stone-200 bg-white p-6"><span className="grid h-11 w-11 place-items-center rounded-xl bg-[#ebe8f5] text-[#50427b]"><LayoutDashboard className="h-5 w-5" /></span><h2 className="mt-5 font-black text-[#173c37]">الإعدادات الحية</h2><p className="mt-2 text-sm leading-7 text-stone-500">عدّل النصوص ووسائل الدفع والكتب، واستخدم مركز التاجر لإدارة المنتجات والمخزون.</p></div></div><StoreContentEditor /><PaymentOperations /><DigitalBookUploader /><section className="mt-7 overflow-hidden rounded-3xl border border-stone-200 bg-white"><div className="flex items-center justify-between border-b border-stone-200 px-6 py-5"><div><h2 className="text-xl font-black text-[#173c37]">الطلبات الواردة</h2><p className="mt-1 text-sm text-stone-500">تظهر الطلبات الجديدة أولًا.</p></div><span className="rounded-full bg-[#e3eee8] px-3 py-1 text-sm font-bold text-[#173c37]">{orders.length}</span></div>{ordersLoading ? <div className="grid min-h-48 place-items-center"><LoaderCircle className="h-6 w-6 animate-spin text-[#b76f2c]" /></div> : orders.length ? <div className="overflow-x-auto"><table className="w-full min-w-[820px] text-right text-sm"><thead className="bg-[#fffdf8] text-stone-500"><tr><th className="px-6 py-4 font-bold">الطلب</th><th className="px-4 py-4 font-bold">العميل</th><th className="px-4 py-4 font-bold">الدفع</th><th className="px-4 py-4 font-bold">الإجمالي</th><th className="px-4 py-4 font-bold">الحالة</th></tr></thead><tbody>{orders.map(order => <tr key={order.id} className="border-t border-stone-100"><td className="px-6 py-4"><strong className="block text-[#173c37]">{order.orderNumber}</strong><span className="mt-1 block text-xs text-stone-400">{new Date(order.createdAt).toLocaleDateString("ar-EG")}</span></td><td className="px-4 py-4"><strong className="block text-[#173c37]">{order.customerName}</strong><span className="mt-1 block text-xs text-stone-500">{order.customerPhone}</span></td><td className="px-4 py-4 text-stone-600">{order.paymentMethod}</td><td className="px-4 py-4 font-bold text-[#173c37]">{Number(order.total).toLocaleString("ar-EG")} {order.currencyCode}</td><td className="px-4 py-4"><select value={order.status} disabled={updateStatus.isPending} onChange={event => updateStatus.mutate({ orderId: order.id, status: event.target.value as (typeof statuses)[number] })} className="rounded-xl border border-stone-200 bg-white px-3 py-2 text-sm font-bold text-[#173c37] outline-none focus:border-[#173c37]">{statuses.map(status => <option key={status} value={status}>{status}</option>)}</select></td></tr>)}</tbody></table></div> : <div className="px-6 py-14 text-center text-sm text-stone-500">لا توجد طلبات مسجلة حتى الآن.</div>}</section></div></StoreLayout>;
}
